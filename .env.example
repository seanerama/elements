# Runtime — required on EC2 (PM2 reads these via env_file)
HOST=127.0.0.1
PORT=8011
NODE_ENV=production

# Pipeline — developer machine only, never committed
# NANO_BANANA_API_KEY=
# WIKIMEDIA_USER_AGENT=Elements/0.1.0 (https://elements.seanmahoney.ai; contact@example.com)
